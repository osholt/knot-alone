services:
  caddy:
    environment:
      TIDE_AND_SEEK_PREPRODUCTION_DOMAIN: ${TIDE_AND_SEEK_PREPRODUCTION_DOMAIN:?set TIDE_AND_SEEK_PREPRODUCTION_DOMAIN}
    volumes:
      - ./Caddyfile.preproduction:/etc/caddy/Caddyfile:ro
